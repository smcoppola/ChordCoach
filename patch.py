import sys

with open("src/logic/services/chord_trainer.py", "r", encoding="utf-8") as f:
    lines = f.readlines()

def replace_block(pattern, start_str, end_str, replacement):
    for i, line in enumerate(lines):
        pass

# Actually I'll just use multi_replace. It's fine if I provide exact matches.
